# Context7 API Key (optional — public rate limits apply without it)
# Export in your shell: export CONTEXT7_API_KEY="your_api_key_here"
# This file is a REFERENCE ONLY — the script does NOT read .env files.
# Real key must be injected via shell/export/CI secret.
