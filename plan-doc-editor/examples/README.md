# Plan Doc Editor — Examples

## Minimal Module

Two ways to start:

- Recommended: generate a fresh module:

```powershell
python scripts/init_plan_skill.py plans <slug> "<title>"
```

- Alternative: copy `minimal-module/` into your workspace (rename the folder, then fill in `CURRENT.md`).

